from icecream import ic

def callback(self, app=None, subapp_components=None):
    # for i in range(5):
    #     app.components.get('due payments').tools.buttons[i].configure(fg_color='white', text_color="white", text='', state='disabled')

    return
